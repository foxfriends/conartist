/*       */
                                                    

                        
             
                 
               
               
                   
               
                        
  

export function parse({ id, typeId, name, sku, quantity, sort, discontinued }                 )          {
  return {
    id,
    typeId,
    name,
    sku,
    quantity,
    sort,
    discontinued,
  }
}
